" rvim's default colorscheme stub.
"
" rvim doesn't yet ship real colorschemes; this file exists so that
" `:colorscheme default` (and the equivalent vim.cmd("colorscheme
" default") from a plugin) doesn't error with E185.
"
" Future: actually define hl groups here once rvim's highlight model
" supports the full set.
